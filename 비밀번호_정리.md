# 비밀번호 정리 가이드

## 🔐 이 프로젝트에 사용되는 비밀번호들

### 1. PostgreSQL 데이터베이스 비밀번호 ⭐ (가장 중요!)

```
비밀번호: your_secure_password_123
사용자명: admin
```

**용도:**
- 애플리케이션 서비스들이 데이터베이스에 연결할 때 사용
- pgAdmin4에서 PostgreSQL 서버 등록할 때 사용
- DBeaver에서 연결할 때 사용
- 모든 서비스(user-service, api-gateway 등)가 이 비밀번호로 DB 접속

**변경하면 안 됩니다!** (애플리케이션 코드에 하드코딩되어 있음)

---

### 2. pgAdmin4 웹 인터페이스 로그인 비밀번호

```
이메일: admin@example.com
비밀번호: admin123
```

**용도:**
- http://localhost:5050 에서 pgAdmin4 웹 인터페이스에 로그인할 때 사용
- PostgreSQL 자체와는 무관하며, pgAdmin4 관리 도구 접속용

---

### 3. password123 (사용 안 함)

```
비밀번호: password123
```

**용도:**
- ~~DBeaver 연결을 위해 임시로 변경했었음~~
- **현재는 사용하지 않음** (원래대로 복원했음)
- 더 이상 필요 없음

---

## 📊 비밀번호 사용 상황 정리

| 비밀번호 | 용도 | 현재 상태 |
|---------|------|----------|
| `your_secure_password_123` | PostgreSQL DB 접속 | ✅ 사용 중 (변경 금지) |
| `admin123` | pgAdmin4 웹 로그인 | ✅ 사용 중 |
| `password123` | (임시로 사용했었음) | ❌ 사용 안 함 |

---

## 🎯 실제 사용 방법

### pgAdmin4에서 PostgreSQL 연결할 때:

1. **pgAdmin4 웹 인터페이스 접속:**
   - URL: http://localhost:5050
   - 이메일: `admin@example.com`
   - 비밀번호: `admin123` ← 이건 pgAdmin4 로그인용

2. **PostgreSQL 서버 등록:**
   - Host: `postgres` 또는 `localhost` (포트 5433)
   - Database: `search_exchange_db`
   - Username: `admin`
   - Password: `your_secure_password_123` ← 이건 PostgreSQL 비밀번호

---

## ⚠️ 중요 사항

### password123은?
- **더 이상 사용하지 않습니다**
- DBeaver 연결 문제를 해결하려고 임시로 사용했었지만
- 원래 비밀번호로 복원했으므로 이제 필요 없음

### 왜 비밀번호가 여러 개?
1. **PostgreSQL DB 비밀번호**: 데이터베이스 자체의 비밀번호
2. **pgAdmin4 로그인 비밀번호**: pgAdmin4 웹 도구 접속용
3. **password123**: 임시 테스트용 (더 이상 사용 안 함)

---

## ✅ 정리

- **데이터베이스 접속**: `your_secure_password_123` 사용
- **pgAdmin4 웹 로그인**: `admin123` 사용  
- **password123**: 사용 안 함 (무시해도 됨)

